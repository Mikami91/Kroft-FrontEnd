// Dependencies
import { makeStyles } from '@material-ui/core';
import { createMuiTheme } from '@material-ui/core/styles';
// Colors
import { amber, green, deepOrange, deepPurple } from '@material-ui/core/colors';

// Theme
export const theme = createMuiTheme({
  palette: {
    type: "light",
  },
});